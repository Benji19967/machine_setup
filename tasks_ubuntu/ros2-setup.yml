# https://docs.ros.org/en/kilted/Installation/Ubuntu-Install-Debs.html
#
# MANUAL FIX ON UBUNTU SERVER: https://github.com/ros2/ros2/issues/1621

- name: Enable Ubuntu Universe repository
  shell: |
    sudo apt install software-properties-common
    sudo add-apt-repository universe
  args:
    executable: /bin/bash
  become: true  # need sudo
  tags:
    - ros2
- name: Install ros2-apt-source package
  shell: |
    sudo apt update && sudo apt install curl -y
    export ROS_APT_SOURCE_VERSION=$(curl -s https://api.github.com/repos/ros-infrastructure/ros-apt-source/releases/latest | grep -F "tag_name" | awk -F'"' '{print $4}')
    curl -L -o /tmp/ros2-apt-source.deb "https://github.com/ros-infrastructure/ros-apt-source/releases/download/${ROS_APT_SOURCE_VERSION}/ros2-apt-source_${ROS_APT_SOURCE_VERSION}.$(. /etc/os-release && echo ${UBUNTU_CODENAME:-${VERSION_CODENAME}})_all.deb"
    sudo dpkg -i /tmp/ros2-apt-source.deb
  args:
    executable: /bin/bash
  become: true  # need sudo
  tags:
    - ros2
- name: Install ros2 base (kilted)
  shell: |
    sudo apt update
    sudo apt upgrade
    sudo apt install ros-jazzy-ros-base
  args:
    executable: /bin/bash
  become: true  # need sudo
  tags:
    - ros2
- name: Setup the environment
  shell: |
    source /opt/ros/jazzy/setup.bash
  args:
    executable: /bin/bash
  tags:
    - ros2
